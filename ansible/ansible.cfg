[defaults]
# Configuration cleared; previous wrapper playbooks removed. New playbooks
# and roles will be added under this directory when conversion begins.
inventory = ./inventory/hosts.ini
roles_path = ./roles
host_key_checking = False
